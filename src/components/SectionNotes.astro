<script>
	import { bootSectionNotes } from "@/lib/notes/client";

	bootSectionNotes();
</script>

<style is:global>
	.section-note-toolbar {
		display: flex;
		justify-content: flex-end;
		margin: 10px 0 18px;
	}

	.section-note-toggle,
	.section-note-close,
	.section-note-form button,
	.section-note-card-actions button {
		border: 1px solid var(--border);
		border-radius: 6px;
		background: var(--bg);
		color: var(--text);
		font: inherit;
		cursor: pointer;
		font-size: 13px;
	}

	.section-note-toggle {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		padding: 6px 12px;
	}

	.section-note-toggle--active {
		border-color: var(--accent);
		color: var(--accent);
	}

	.section-note-count {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-width: 1.4rem;
		height: 1.4rem;
		padding: 0 0.3rem;
		border-radius: 4px;
		background: var(--code-bg);
		font-size: 0.78rem;
	}

	.section-note-panel {
		margin: 0 0 24px;
		padding: 16px;
		border: 1px solid var(--border);
		border-radius: 8px;
		background: var(--bg);
	}

	.section-note-panel-head {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 12px;
		margin-bottom: 14px;
	}

	.section-note-panel-head strong {
		display: block;
		margin-bottom: 2px;
		font-size: 14px;
	}

	.section-note-panel-head p {
		margin: 0;
		font-size: 13px;
		color: var(--text-secondary);
	}

	.section-note-close,
	.section-note-card-actions button,
	.section-note-form button[type="button"] {
		padding: 6px 10px;
	}

	.section-note-list {
		list-style: none;
		margin: 0 0 14px;
		padding: 0;
		display: grid;
		gap: 8px;
	}

	.section-note-card {
		padding: 12px 14px;
		border-radius: 6px;
		background: var(--code-bg);
		border: 1px solid var(--border);
	}

	.section-note-card-head {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 12px;
		margin-bottom: 6px;
	}

	.section-note-card time {
		color: var(--text-tertiary);
		font-size: 12px;
	}

	.section-note-card-actions {
		display: flex;
		gap: 6px;
	}

	.section-note-card p,
	.section-note-empty {
		margin: 0;
		font-size: 14px;
		line-height: 1.6;
		color: var(--text);
	}

	.section-note-empty {
		margin-bottom: 14px;
		color: var(--text-tertiary);
	}

	.section-note-form {
		display: grid;
		gap: 10px;
	}

	.section-note-form label {
		display: grid;
		gap: 6px;
		font-size: 13px;
		color: var(--text-secondary);
	}

	.section-note-form textarea {
		width: 100%;
		min-height: 100px;
		padding: 10px 12px;
		border: 1px solid var(--border);
		border-radius: 6px;
		background: var(--bg);
		color: var(--text);
		font: inherit;
		font-size: 14px;
		resize: vertical;
	}

	.section-note-form textarea:focus {
		outline: none;
		border-color: var(--accent);
		box-shadow: 0 0 0 2px rgba(61, 107, 126, 0.12);
	}

	.section-note-form-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 8px;
		justify-content: flex-end;
	}

	.section-note-form button[type="submit"] {
		padding: 8px 14px;
		background: var(--accent);
		border-color: var(--accent);
		color: #fff;
	}

	.section-note-form button[type="submit"]:hover {
		background: var(--accent-hover);
	}
</style>
