<script>
	import { bootSectionNotes } from "@/lib/notes/client";

	bootSectionNotes();
</script>

<style is:global>
	.section-note-toolbar {
		display: flex;
		justify-content: flex-end;
		margin: 10px 0 18px;
	}

	.section-note-toggle,
	.section-note-close,
	.section-note-form button,
	.section-note-card-actions button {
		border: 1px solid rgba(148, 163, 184, 0.18);
		border-radius: 999px;
		background: rgba(148, 163, 184, 0.06);
		color: var(--text);
		font: inherit;
		cursor: pointer;
	}

	.section-note-toggle {
		display: inline-flex;
		align-items: center;
		gap: 10px;
		padding: 8px 12px;
	}

	.section-note-toggle--active {
		border-color: rgba(245, 158, 11, 0.3);
		background: rgba(245, 158, 11, 0.12);
	}

	.section-note-count {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-width: 1.6rem;
		height: 1.6rem;
		padding: 0 0.35rem;
		border-radius: 999px;
		background: rgba(2, 6, 23, 0.5);
		font-size: 0.82rem;
	}

	.section-note-panel {
		margin: 0 0 24px;
		padding: 18px;
		border: 1px solid rgba(148, 163, 184, 0.16);
		border-radius: 20px;
		background: rgba(9, 17, 26, 0.48);
	}

	.section-note-panel-head {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 12px;
		margin-bottom: 16px;
	}

	.section-note-panel-head strong {
		display: block;
		margin-bottom: 4px;
	}

	.section-note-panel-head p {
		margin: 0;
		font-size: 0.95rem;
	}

	.section-note-close,
	.section-note-card-actions button,
	.section-note-form button[type="button"] {
		padding: 8px 12px;
	}

	.section-note-list {
		list-style: none;
		margin: 0 0 18px;
		padding: 0;
		display: grid;
		gap: 12px;
	}

	.section-note-card {
		padding: 14px 16px;
		border-radius: 16px;
		background: rgba(148, 163, 184, 0.06);
		border: 1px solid rgba(148, 163, 184, 0.12);
	}

	.section-note-card-head {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 12px;
		margin-bottom: 8px;
	}

	.section-note-card time {
		color: var(--muted);
		font-size: 0.85rem;
	}

	.section-note-card-actions {
		display: flex;
		gap: 8px;
	}

	.section-note-card p,
	.section-note-empty {
		margin: 0;
		font-family: var(--sans);
		font-size: 0.95rem;
		line-height: 1.65;
		color: rgba(248, 250, 252, 0.92);
	}

	.section-note-empty {
		margin-bottom: 18px;
		color: var(--muted);
	}

	.section-note-form {
		display: grid;
		gap: 12px;
	}

	.section-note-form label {
		display: grid;
		gap: 8px;
		font-size: 0.92rem;
		color: var(--muted);
	}

	.section-note-form textarea {
		width: 100%;
		min-height: 110px;
		padding: 12px 14px;
		border: 1px solid rgba(148, 163, 184, 0.18);
		border-radius: 16px;
		background: rgba(2, 6, 23, 0.48);
		color: var(--text);
		font: inherit;
		resize: vertical;
	}

	.section-note-form textarea:focus {
		outline: 1px solid rgba(245, 158, 11, 0.45);
		border-color: rgba(245, 158, 11, 0.45);
	}

	.section-note-form-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 10px;
		justify-content: flex-end;
	}

	.section-note-form button[type="submit"] {
		padding: 10px 14px;
		border-color: rgba(245, 158, 11, 0.35);
		background: rgba(245, 158, 11, 0.14);
	}
</style>
